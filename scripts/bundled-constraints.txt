# Bundled dependencies — version constraints for cross-platform reproducibility
# These are platform-independent upper/lower bounds for critical transitive deps.
# Generated by scripts/bundle-agent-deps.sh after native install.
# 
# To regenerate: FORCE_RELOCK=1 bash scripts/bundle-agent-deps.sh
#
# websockets: hermes-agent 0.16.0 (submodule c6b0eb4) pins websockets==15.0.1
# directly. The previous dingtalk-stream-only rationale for <14 is no longer
# applicable — dingtalk-stream 0.24.x works fine with websockets 15.x. If we
# pin <14 here the bundle step dies with "ResolutionImpossible".
websockets>=13,<16
